name: "external API action"

description: "use an external API to retrieve and display a joke.js"

outputs:
  joke-output:
    description: The resulting joke.js from the icanhazdadjokes API

runs:
  using: "node12"
  main: "main.js"
